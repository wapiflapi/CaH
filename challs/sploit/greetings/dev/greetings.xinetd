service greetings
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = someuser
    bind        = 0.0.0.0
    server      = /path/to/greetings/greetings
    type        = UNLISTED
    port        = 9999
}
